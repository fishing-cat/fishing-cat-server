<!DOCTYPE html>
<html>
  <head>
    <title>Login</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'fishing', media:'all' %>
    <%= javascript_include_tag 'fishing' %>
  </head>
  <body>
    <div class="container">
      <h2>Login</h2>
      <p>Welcome. Please enter your personal information in the fields below.</p>
      <div class="form-horizontal">
        <%= form_tag do |f| %>
        <%= hidden_field_tag :cid, params[:cid] %>
        <%= hidden_field_tag :pid, params[:pid] %>
        <div class="form-group">
          <%= label_tag :name, 'User Name', class:'col-sm-2 control-label' %>
          <div class="col-sm-10">
            <%= text_field_tag '', '', id:'name', class:'form-control' %>
          </div>
        </div>
        <div class="form-group">
          <%= label_tag :password, 'User Password', class:'col-sm-2 control-label' %>
          <div class="col-sm-10">
            <%= password_field_tag '', '', id:'password', class:'form-control' %>
          </div>
        </div>
        <div class="form-group">
          <div class="col-md-offset-2 col-sm-10">
            <%= submit_tag 'Login', class:'btn btn-primary' %>
          </div>
        </div>
        <% end %>
      </div>
    </div>
  </body>
</html>
