databases:
  - name: fishing-cat-server
    databaseName: fishing-cat-server
    user: fishing-cat-server
services:
  - name: fishing-cat-server
    type: web
    runtime: ruby
    buildCommand: "./bin/render-build.bash"
    startCommand: "bin/rails server -p ${PORT:-5000} -e production"
    autoDeploy: false
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: fishing-cat-server
          property: connectionString
      - key: SECRET_KEY_BASE
        generateValue: true